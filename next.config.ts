/**
 * Hackathon Submission Note: Next.js config for the demo workspace.
 * (No functional changes made here.)
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
